<%= form_for(intequipment) do |f| %>
  <% if intequipment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(intequipment.errors.count, "error") %> prohibited this intequipment from being saved:</h2>

      <ul>
      <% intequipment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
